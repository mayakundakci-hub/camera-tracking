import QtQuick
import Mu.Material
import CameraTracking


MuCard {
    background: Rectangle {
        color: Theme.surface
        radius: Theme.radiusLarge
        border.color: Theme.outline
        border.width: 1
    }
}
